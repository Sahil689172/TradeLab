fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0,<1.0.0
sqlalchemy>=2.0.36,<3.0.0
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0
python-dotenv>=1.0.1,<2.0.0
pyyaml>=6.0.0,<7.0.0

httpx>=0.27.0,<1.0.0
websockets>=12.0,<16.0
pandas>=2.2.0,<3.0.0
pyarrow>=18.0.0,<20.0.0
yfinance>=0.2.54,<1.0.0
matplotlib>=3.8.0,<4.0.0
numpy>=1.26.0,<3.0.0

# Test dependencies. pytest-asyncio is REQUIRED: pytest.ini sets
# asyncio_mode=auto, and without this plugin every async test (including the
# whole Monte Carlo streaming suite) is silently skipped as "async def
# functions are not natively supported" rather than actually running.
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.24.0,<2.0.0
